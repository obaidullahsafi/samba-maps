First create map like code.
sudo mkdir code
after we clome our file from github.
git clone https://github.com/obaidullahsafi/ateljee_samba.git
after we will go to our map ateljee_samba
ls and we will see our file samba_automatiion_ubuntu.sh 
we will go back to cd code/ateljee_samba
 then cd /srv
 we will create the map like 
sudo mkdir shares
after cd shares
and we will create maps like 
sudo mkdir DTP
 sudo mkdir workshops
sudo mkdir public
sudo mkdir admin
lets give this premission to run 
cd code/ateljee_samba
chmod +rwx samba_automatiion_ubuntu.sh 
and then sudo ./ samba_automatiion_ubuntu.sh 
